$key = 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects'
$entry = (Get-ItemProperty -Path $key -Name 'VisualFXSetting' -ErrorAction SilentlyContinue)
if($entry.'VisualFXSetting' -ne 2) { exit 1 }